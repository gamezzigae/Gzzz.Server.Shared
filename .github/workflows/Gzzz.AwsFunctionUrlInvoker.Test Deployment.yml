name: unit test

on:
  push:
    branches: [ "main"]
    paths:
      - "src/Gzzz.AwsFunctionUrlInvoker.Test/**"

env:
    NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    PROJECT_PATH: ./src/Gzzz.AwsFunctionUrlInvoker.Test/Gzzz.AwsFunctionUrlInvoker.Test.csproj
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
        source-url: https://nuget.pkg.github.com/gamezzigae/index.json

        
    - name: Set up Docker Compose
      run: sudo apt-get install docker-compose

    - name : Run Docker Images
      run: docker-compose -f ./docker-compose.yml up -d
      
    - name: Restore
      run: dotnet restore ./src

    - name: Build
      run: dotnet build ./src --configuration Release --no-restore

    - name: Test
      run: dotnet test ./src --configuration Release --no-restore
      