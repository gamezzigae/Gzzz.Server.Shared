version: '3.1'
services:
    dynamodb:
        image: amazon/dynamodb-local:2.2.1
        restart: always
        ports:
            - "8000:8000"

    valkey:
        image: valkey/valkey:8.0.3-alpine
        restart: always
        ports:
            - "6379:6379"
